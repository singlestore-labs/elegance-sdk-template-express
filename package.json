{
  "name": "elegance-sdk-template-express",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "dotenv -- npm-run-all --parallel build:*",
    "build:client": "dotenv -- npm --prefix ./client run build",
    "build:server": "dotenv -- npm --prefix ./server run build",
    "dev": "dotenv -- npm-run-all --parallel dev:*",
    "dev:client": "dotenv -- npm --prefix ./client run dev",
    "dev:server": "dotenv -- npm --prefix ./server run dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "workspaces": [
    "./client",
    "./server"
  ],
  "devDependencies": {
    "dotenv-cli": "^7.3.0",
    "npm-run-all": "^4.1.5"
  }
}
